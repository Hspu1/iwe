[project]
name = "pg-seeding"
version = "0.1.0"
description = "whatcha wanna know huh"
readme = "README.md"
requires-python = ">=3.12"

dependencies = []

[build-system]
requires = ["uv-build"]
build-backend = "uv_build"
